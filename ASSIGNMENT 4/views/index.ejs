<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bareeze</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
      /* PAGE 1 HOME PAGE */
      html, body {
    overflow-x: hidden;
     }
      body {
        margin: 0;
      }
      .nav-container {
        position: relative;
        height: 100vh;
        width: 100vw;
        margin: 0;
        padding: 0;
         background-color: #f0f0f0; 
      }
      .homepageimg-fluid {
        display: block;
        width: 100%;
        height: 100%;
        object-fit: cover;
      }
      .nav {
    
        position: fixed;
        top: 10px; 
        right: 10px; 
        display: flex;
        align-items: center;
        z-index: 1000;
      }
      .nav ul {
        list-style: none;
        display: flex;
        gap: 20px;
        padding: 0;
        margin: 0;
      }
      .nav ul li {
        display: inline;
      }
      .nav ul li a {
        color: white ;
        font-size: medium;
        font-weight: bold;
        text-decoration: none;
      }
      .nav ul li a:hover {
        color: #f0f0f0 ;
      }
      .nav-link{
        color:white;
      }
      .heading {
        position: fixed;
        top: 10px;
        left: 10px;
        color: white;
        font-size: 3em;
        font-weight: lighter;
        font-style: italic;
        z-index: 1000;
      }
      .first-image-container{
        height: 100vh;
        width: 100vw;

      }  

      .first-image-fluid{
        display: block;
        width: 100%;
        height: 100vh;
        object-fit: cover;
      }
      /* Second home page image */
      .second-image-container {
        height: 100vh;
        width: 100vw;
      }
      .second-image-fluid {
        display: block;
        width: 100%;
        height: 100vh;
        object-fit: cover;
      }
      
      /* Third home page image */
      .third-image-container {
        height: 100vh;
        width: 100vw;
      }
      .third-image-fluid {
        display: block;
        width: 100%;
        height: 100vh;
        object-fit: cover;
      }

      /* Fourth home page image */
      .fourth-image-container {
        height: 100vh;
        width: 100vw;
      }
      .fourth-image-fluid {
        display: block;
        width: 100%;
        height: 100vh;
        object-fit: cover;
      }
       /*  Fifth Video page */
      
      .fifth-video-container {
        height: 100vh;
        width: 100vw;
        display: flex; 
        align-items: center;
        justify-content: center;
        overflow: hidden; 
        background-color: black;
     }

      .fifth-video-fluid {
        width: 100%;
        height: 100%;
        object-fit: cover; 
      }

      /* Full screen footer section */
      .footer-section {
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: #f8f9fa;
      }
      .footer-section .row {
        width: 100%;
        max-width: 1200px;
      }
      .footer-section h5 {
        font-weight: bold;
      }
      .footer-section .input-group {
        margin-top: 10px;
      }
      .footer-section a {
        text-decoration: none;
        color: #000;
      }
    /* General Header Styles */
header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 1000;
    background-color:transparent; 
    display: flex;
    flex-direction: column;
    border:none;
    padding:0;
    margin:0;
    box-shadow:none;

}

.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 20px;
    color: white;
    border-radius: 0; 
    box-shadow: none; 
    border: none;
}

/* Menu Icon and Brand Name */
.menu-container {
    display: flex;
    align-items: center;
    gap: 10px; /* Space between menu icon and brand name */
}

.menu-icon {
    font-size: 24px;
    cursor: pointer;
}

.brand-name {
    margin: 0;
    font-size: 1.5em;
    font-weight: bold;
    color: white;
}

/* Nav Links (Search, Track Orders, Shopping Cart) */
.nav-links {
    display: flex;
    gap: 20px;
}

.nav-links a {
    color: white;
    text-decoration: none;
    font-size: 1em;
}

.nav-links a:hover {
    color: #f1c40f;
}

/* Menu Overlay */
.menu-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5); /* Semi-transparent background */
    color: white;
    display: none;
    flex-direction: column;
    justify-content: space-around;
    align-items: center;
    z-index: 9999;
}

.menu-overlay.active {
    display: flex;
}

.menu-overlay .menu-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr); /* Three columns */
    gap: 20px;
    text-align: center;
}

.menu-overlay a {
    color: white;
    text-decoration: none;
    font-size: 1.5em;
}

.menu-overlay a:hover {
    color: #f1c40f;
}

.close-btn {
    position: absolute;
    top: 10px;
    right: 20px;
    font-size: 2em;
    cursor: pointer;
    color: white;
}

/* Main content offset for fixed header */
.page-content {
    margin-top: 80px; /* Adjust this to ensure no content overlaps the fixed header */
}

    </style>
  </head>
  <body>
    <!-- PAGE 1 HOME PAGE -->
    <div class="first-image-container">
      <a href="Embroidedclassics.html">
        <img src="https://cdn.comverseglobal.com/bareeze/products/product_images/image-33x.jpg" class="first-image-fluid" alt="Error">
      </a>
    </div>

    <header>
      <div class="navbar">
          <div class="menu-container">
              <span class="menu-icon" onclick="toggleMenu()">☰</span>
              <h1 class="brand-name">Bareeze</h1>
          </div>
          <div class="nav-links">
              <a href="#">Search</a>
              <a href="#">Track Orders</a>
              <a href="#">Shopping Cart</a>
          </div>
          
      </div>
      <div id="menu" class="menu-overlay">
          <div class="menu-grid">
            
              <a href="Embroidedclassics.html">EMBROIDED CLASSICS</a>
              <a href="Premiumprints.html">PREMIUM PRINTS</a>
              <a href="FestiveCollection.html">FESTIVE COLLECTION</a>
              <a href="Shawls.html">SHAWLS</a>
              <a href="bottom.html">BOTTOMS</a>
              <a href="Bareeze man.html">BAREEZE MEN</a>
      
          </div>
          <span class="close-btn" onclick="toggleMenu()">×</span>
      </div>
  </header>
  

    <!-- PAGE 2 HOME PAGE -->
    <div class="second-image-container">
      <a href="Premiumprints.html">
        <img src="https://cdn.comverseglobal.com/bareeze/products/product_images/image-22x.jpg" class="second-image-fluid" alt="Error">
      </a>
    </div>

    <!-- PAGE 3 HOME PAGE -->
    <div class="third-image-container">
      <a href="FestiveCollection.html">
        <img src="https://cdn.comverseglobal.com/bareeze/products/product_images/image-44x.jpg" class="third-image-fluid" alt="Error">
      </a>
    </div>

    <!-- PAGE 4 HOME PAGE -->
    <div class="fourth-image-container">
      <a href="Shawls.html">
        <img src="https://cdn.comverseglobal.com/bareeze/products/product_images/image-11x.jpg" class="fourth-image-fluid" alt="Error">
      </a>
    </div>
     <!-- Video  -->
     <div class="fifth-video-container">
    <video class="fifth-video-fluid" autoplay muted loop controls>
        <source src="https://cdn.comverseglobal.com/bareeze/products/product_images/website-1660x720.mp4" type="video/mp4">
        Your browser does not support the video tag.
    </video>
</div>


<div class="main">
  <ul>
      <% for (let index = 0; index < product.length; index++) { %>
          <li>
              <img src="/<%= product[index].picture %>" alt="Product Image" style="max-width: 500px; max-height: 500px;">
              <div class="product-name">
                  <strong>Name:</strong> <%= product[index].name %>
              </div>
              <div>
                  <strong>Price:</strong> <%= product[index].price %> <br>
              </div>
              <% if (product[index].description) { %>
                  <div>
                      <strong>Description:</strong> <%= product[index].description %>
                  </div>
              <% } %>
              
              <!-- Add to Cart Form -->
              <form action="/cart/add" method="POST">
                  <input type="hidden" name="productId" value="<%= product[index].id %>">
                  <button type="submit" class="btn btn-primary">
                      <i class="fa-solid fa-cart-plus"></i> Add to Cart
                  </button>
              </form>
          </li>
      <% } %>
  </ul>
</div>
  
    <!-- LAST PAGE FOOTER SECTION -->
    <div class="footer-section">
      <div class="row text-center">
        <div class="col-md-3">
          <h5>CONTACT US</h5>
          <p>Bareeze Retail Head Office<br>1.5-Km, Defence Road, Bhobtian Chowk, Lahore</p>
          <p>wecare@bareezeonline.pk</p>
          <p>+92(0)42 111-738-245</p>
        </div>
        <div class="col-md-3">
          <h5>CUSTOMER CARE</h5>
          <p><a href="#">Exchange & Return Policy</a></p>
          <p><a href="#">FAQs</a></p>
          <p><a href="#">Contact Us</a></p>
        </div>
        <div class="col-md-3">
          <h5>INFORMATION</h5>
          <p><a href="#">About Us</a></p>
          <p><a href="#">Privacy Policy</a></p>
          <p><a href="#">Exchange Policy</a></p>
        </div>
        <div class="col-md-3">
          <h5>NEWSLETTER SIGNUP</h5>
          <p>Subscribe to our Newsletter for Updates</p>
          <div class="input-group">
            <input type="email" class="form-control" placeholder="Your email address">
            <button class="btn btn-dark" type="button">Subscribe</button>
          </div>
          <div class="mt-3">
            <a href="#" class="text-dark me-3"><i class="fab fa-facebook-f"></i></a>
            <a href="#" class="text-dark me-3"><i class="fab fa-instagram"></i></a>
            <a href="#" class="text-dark"><i class="fab fa-youtube"></i></a>
          </div>
        </div>
      </div>
    </div>
    

    <script src="menu.js" defer></script>
    <script>
      // Video will play automatically 
    
   document.addEventListener("DOMContentLoaded", () => {
    const video = document.querySelector(".fifth-video-fluid");

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            if (!video.playing) {
              video.play().catch((error) => {
                console.log("Autoplay prevented:", error);
              });
            }
          } else {
            video.pause();
          }
        });
      },
      { threshold: 0.5 }
    );

    observer.observe(video);

    // Enable autoplay once user interacts with the page
    const enableAutoplay = () => {
      video.play().catch((error) => {
        console.log("Autoplay prevented:", error);
      });
      document.removeEventListener("click", enableAutoplay);
      document.removeEventListener("scroll", enableAutoplay);
    };

    document.addEventListener("click", enableAutoplay);
    document.addEventListener("scroll", enableAutoplay);
  });
      // Show the search box when the search button is clicked
document.getElementById("searchBtn").addEventListener("click", function () {
    const searchBox = document.getElementById("searchBox");
    searchBox.classList.toggle("hidden"); // Toggle visibility of search box
});

// Handle search button click to perform the search
document.getElementById("searchSubmit").addEventListener("click", function () {
    const searchQuery = document.getElementById("searchInput").value.trim();

    if (searchQuery) {
        // Redirect to the search results page (or update the current page to display the results)
        window.location.href = `/search?query=${encodeURIComponent(searchQuery)}`;
    } else {
        alert("Please enter a search term.");
    }
});

// Optional: Handle "Enter" key press in the search input for accessibility
document.getElementById("searchInput").addEventListener("keypress", function (e) {
    if (e.key === 'Enter') {
        document.getElementById("searchSubmit").click(); // Trigger search on Enter key press
    }
});
  // Add to Cart
  document.querySelectorAll('.add-to-cart-btn').forEach((button) => {
        button.addEventListener('click', () => {
            const productId = button.getAttribute('data-product-id');

            fetch('/cart/add', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify({ productId }),
            })
                .then((response) => response.json())
                .then((data) => {
                    if (data.success) {
                        alert(data.message); // Display success message
                        updateCartCount(data.cartCount); // Update cart count dynamically
                    } else {
                        alert('Error adding product to cart.');
                    }
                })
                .catch((error) => {
                    console.error('Error adding product to cart:', error);
                    alert('Error adding product to cart.');
                });
        });
    });

    function updateCartCount(count) {
        const cartCountElement = document.querySelector('#cart-count');
        if (cartCountElement) {
            cartCountElement.textContent = count;
        }
    }
</script>   
  </body>
</html>
